# GNN優秀人材分析システム - 設定ファイル

# GNNモデルの設定
model:
  n_layers: 3
  hidden_dim: 128
  dropout: 0.3
  learning_rate: 0.01
  k_neighbors: 10  # グラフ構築時の近傍数

# 学習の設定
training:
  default_epochs: 100
  min_epochs: 50
  max_epochs: 500
  early_stopping_patience: 20
  batch_size_edges: 1000  # エッジサンプリング数

# 分析の設定
analysis:
  min_excellent_members: 3
  max_excellent_members_recommended: 20
  top_skills_display: 50
  essential_skill_threshold: 0.8  # 必須スキルとみなす保有率
  important_skill_diff_threshold: 0.3  # 重要スキルとみなす差分
  significant_skill_diff_threshold: 0.2  # 有意とみなす差分

# データカラム名マッピング
column_names:
  member:
    code: "メンバーコード  ###[member_code]###"
    name: "メンバー名  ###[name]###"
    retired_day: "退職年月日  ###[retired_day]###"
    enter_day: "入社年月日  ###[enter_day]###"
    job_grade: "職能・等級  ###[job_grade]###"
    job_position: "役職  ###[job_position]###"

  acquired:
    member_code: "メンバーコード"
    competence_type: "力量タイプ  ###[competence_type]###"
    competence_code: "力量コード"
    competence_name: "力量名"
    level: "レベル"

  competence_types:
    skill: "SKILL"
    education: "EDUCATION"
    license: "LICENSE"

# 役職マッピング
position_mapping:
  "部長": 4
  "課長": 3
  "係長": 2
  "班長": 1
  "職長": 1

# UI設定
ui:
  page_title: "GNN優秀人材分析システム"
  page_icon: "🎯"

  # カラーパレット
  colors:
    excellent_group: "#FF6B6B"
    non_excellent_group: "#4ECDC4"
    primary: "#FF6B6B"
    secondary: "#4ECDC4"

  # 表示設定
  display:
    top_skills_chart: 15
    member_scores_height: 400
    chart_height: 600
    histogram_bins: 20

  # エポック推奨値
  epoch_recommendations:
    small_group: [50, 100]  # 5名以下
    medium_group: [100, 200]  # 10名程度
    large_group: [200, 300]  # 20名以上

# ファイル設定
files:
  encoding: "utf-8-sig"
  export:
    skill_importance: "skill_importance.csv"
    member_scores: "member_scores.csv"

# 統計的検定の設定
statistical_tests:
  significance_level: 0.05  # 有意水準
  multiple_testing_correction: "fdr_bh"  # 多重検定補正法: "fdr_bh", "bonferroni", "none"
  min_sample_size_warning: 30  # サンプル数の警告閾値
  show_confidence_intervals: true
  confidence_level: 0.95  # 信頼区間の水準

# 因果推論の設定
causal_inference:
  enabled: true
  propensity_score:
    caliper: 0.1  # マッチングの許容範囲（0.05-0.2推奨）
    min_matched_pairs: 5  # 最低マッチングペア数
  confounders:
    use_years_of_service: true
    use_grade: true
    use_position: true
    use_skill_count: true
  show_in_ui: true

# スキル相互作用分析の設定
skill_interaction:
  enabled: true
  synergy_threshold: 0.1  # 相乗効果の閾値（10%以上）
  min_both_rate: 0.7  # 両方保有時の最低優秀率
  min_group_size: 3  # グループの最小人数
  min_skill_samples: 5  # スキル保有者の最小数
  max_pairs_to_analyze: 1000  # 分析する組み合わせの上限（計算時間削減）
  show_in_ui: true

# モデル評価の設定
evaluation:
  enabled: true
  method: "holdout"  # "holdout", "loocv", "both"
  holdout:
    test_ratio: 0.2  # テストデータの割合
    random_seed: 42
    min_test_samples: 1
  loocv:
    enabled_when_small_sample: true  # 少数サンプル時に自動でLOOCVを使用
    small_sample_threshold: 10  # この数以下の場合にLOOCVを使用
  metrics:
    - "auc"
    - "precision"
    - "recall"
    - "f1"
  show_in_ui: true
  warn_overfitting_threshold: 0.2  # train/testのAUC差分がこの値を超えたら警告

# バージョン管理
versioning:
  enabled: true
  save_models: true
  model_dir: "./models"
  log_dir: "./logs"
  include_metadata: true  # メタデータ（パラメータ、日時など）を保存

# ロギング設定
logging:
  level: "INFO"
  format: "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
  file_logging: true
  log_file: "talent_analyzer.log"
  max_bytes: 10485760  # 10MB
  backup_count: 5
  console_logging: true

# 数値計算の設定
numerical:
  epsilon: 1.0e-8  # ゼロ除算防止用の小さな値
